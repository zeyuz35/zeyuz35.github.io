## 2024-05-19 - ARIA labels for icon-only buttons

**Learning:** Buttons containing only icons (like the hamburger menu in the masthead) need `aria-label`s for screen reader users to understand their purpose, since they have no visible text.
**Action:** Always include a descriptive `aria-label` on `<button>` elements that only contain icons.

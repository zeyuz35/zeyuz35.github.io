## 2026-03-04 - Added ARIA labels to navicon and follow buttons\n**Learning:** This Jekyll template relies on icon-only buttons (like the mobile navicon) and buttons with ambiguous text (like 'Follow') without providing accessible names, creating barriers for screen reader users.\n**Action:** Always verify that buttons containing only icons or ambiguous text have an explicit `aria-label` added to ensure they communicate their purpose to assistive technologies.

## 2026-05-19 - Modernizer memory initialization
**Learning:** The repository uses Jupyter Notebooks and Python scripts to generate markdown files.
**Action:** Use `replace_with_git_merge_diff` for Python files and a separate Python script with `json` module to safely modify notebook code cells without disrupting JSON formatting.

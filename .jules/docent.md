## 2024-05-30 - Replace raw print statements with structured logging
**Learning:** The Python scripts in markdown_generator (`pubsFromBib.py` and its corresponding Jupyter notebook) use `print()` statements for diagnostic output which are unorganized and difficult to filter.
**Action:** Replace `print` statements with standard Python `logging` to ensure standard, professional output streams.

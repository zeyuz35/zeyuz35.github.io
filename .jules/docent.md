## 2024-05-21 - Replace informal language and correct typos in markdown_generator scripts
**Learning:** Found several instances of informal language (e.g., "heavy lifting is done", "concatentate", "don't", "doesn't look pretty") and typos ("SUCESSFULLY") in the Python scripts and Jupyter notebooks within the `markdown_generator` directory. The scripts need to be updated in both the `.py` files and the `.ipynb` files to ensure synchronization. When modifying Jupyter Notebooks programmatically, it's safer to read the JSON structure, apply the string replacements specifically on the cell source code, and rewrite the JSON to avoid structural corruption.
**Action:** Created a script (`update_docs.py`) to programmatically update both `.py` and `.ipynb` files within the `markdown_generator` directory, replacing informal language with professional, terse alternatives and correcting typos, ensuring we do not mess up JSON structures. Verified by running the markdown generator scripts successfully.

## 2024-06-14 - Replace informal language in docs and ipynb files
**Learning:** Found informal language like "messed up", "picky", and a typo ("equivilents") in Jupyter Notebooks and python script files (`talks.ipynb`, `publications.ipynb`, `talks.py`, `publications.py`).
**Action:** Replaced these with professional alternatives like "may be parsed incorrectly" and "requires strict string formatting". Corrected the typo to "equivalents". Kept the python script equivalent files in sync with notebooks.

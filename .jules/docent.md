## YYYY-MM-DD - Enforce structured logging in pubsFromBib generator scripts
**Learning:** Replaced raw `print` statements (with capitalized informal phrasing) with proper structured `logging` module in python and jupyter scripts to improve script maintainability.
**Action:** Always prefer `logging.info` and `logging.warning` over raw prints for script outputs.
